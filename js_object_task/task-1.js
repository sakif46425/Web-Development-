const color = {
    red : '#FF0000',
    green : '#00FF00',
    blue : '#0000FF',
    "golden rod" : '#DAA520',
};

console.log(color["golden rod"]); 

console.log(color.red);// This will throw an error because 'golden rod' is not a valid identifier  